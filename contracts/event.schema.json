{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ghostatlas.invalid/contracts/packet-os/event/v1",
  "title": "Packet OS Event v1",
  "type": "object",
  "required": ["event_id", "packet_id", "event_type", "source", "timestamp", "payload"],
  "properties": {
    "event_id": {"type": "string"},
    "packet_id": {"type": "string"},
    "event_type": {"type": "string"},
    "source": {"type": "string"},
    "target": {"type": ["string", "null"]},
    "timestamp": {"type": "string"},
    "payload": {"type": "object"}
  },
  "additionalProperties": false
}
